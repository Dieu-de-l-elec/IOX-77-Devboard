The Arduino library is packed in the `IOX.zip`. It can be downloaded raw and added in the Arduino IDE.
The `IOX` folder contains the different files present inside the compressed `IOX.zip` library, so it's easier to see what's inside...
