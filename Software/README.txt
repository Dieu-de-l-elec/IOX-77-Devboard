Final code doming soon... (I'm currently on Advancement 1)

I'm planning to code an Arduino® library for the ESP32 C3 in order to easily control the 'external' GPIOs (handling I2C com with the 5 CH32s) and be able to use the GPIOs like we do with any devboard (pinMode(), digitalWrite(), digitalRead(), analogRead(), analogWrite() functions ...). I'll also need to programm the CH32, I'm using the MounRiver Studio here to get full command on these MCUs.
I'm coding the CH32 and ESP32, implementing as many functions as I can, but it takes time. I'll build a library once I think it's reliable and complete enough for the IOX-77 to be used on its own, like with any other devboard.
